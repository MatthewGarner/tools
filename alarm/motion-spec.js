/* alarm's two distribution curves are the hero stroke-draw: the only
   fill="none" stroked paths in the distributions SVG. The filled areas use
   fill+fill-opacity (excluded); the threshold is a dashed <line> (excluded /
   fades). No #distwrap prefix — revealIn already scopes to the container's svg. */
export const REVEAL = {draw: 'path[fill="none"][stroke]'};
