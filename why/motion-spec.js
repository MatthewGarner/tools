/* OST connectors draw (fill=none undashed); map-mode shares renderRoadmap → its dashed pills filter out to fade. One selector serves both. */
export const REVEAL = {draw: 'path[fill="none"][stroke]', hold: 'rect[rx]'};
