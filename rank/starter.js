/* The "Start your own" skeleton, in rank's native shape rather than a DSL string:
   three initiatives with a deliberate spread of scores. Identical rows never
   re-sort, so a flat 5/5/5/5 skeleton would hide the very mechanism the tool is
   for — the weights have to visibly move something. Names are obviously yours to
   replace. Kept DOM-free so dev/starter.test.mjs can check its shape. */
export const STARTER = {
  k: 2,
  items: [
    ['Your first initiative',  7, 5, 4, 4],
    ['Your second initiative', 5, 7, 5, 3],
    ['Your third initiative',  6, 4, 7, 6],
  ],
};
