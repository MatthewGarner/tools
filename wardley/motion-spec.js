/* fade-only reveal: wardley's edges are stroke-opacity .45 (too faint to carry a stroke-draw — Fable predicted, screenshot confirmed). Value chain fades in. */
export const REVEAL = {draw: '', hold: 'rect[rx]'};
