/* fade-only reveal: cycles' SoC/soh curves are thin on a large chart — too subtle to carry a draw-lead (blank gap); the chart fades in cleanly instead. */
export const REVEAL = {draw: '', hold: 'rect[rx]'};
