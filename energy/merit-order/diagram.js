/* Public pure stack diagram used by Intraday's live and exported readings.
   Catalogue keys and fuel ordering retain the same meaning in both tools. */
export {renderStack, MERIT_PALETTE, THERMAL_ORDER} from './render.js';
