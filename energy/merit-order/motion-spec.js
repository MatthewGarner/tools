/* merit-order's demand/clearing lines are DASHED (reference language) → they
   fade, not draw; bars (g[data-plant]) fade in then FLIP-glide on edit. This is
   the FLIP-extraction flagship, not a stroke-draw one. Empty draw = pure fade
   reveal (the on-brand default). Hold the chart card so the plate doesn't slide. */
export const REVEAL = {draw: '', hold: 'rect[rx]'};
